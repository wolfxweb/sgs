<div class="{{$classDiv1}}">
    <div class="{{$classDiv2}}">
        <input id="{{$id}}" type="{{$type}}" name="{{$name}}" class="{{$classInput}}" placeholder="{{$placeholder}}" value="{{$value}}">
        <label for="$id">{{$label}}</label>
    </div>
</div>
